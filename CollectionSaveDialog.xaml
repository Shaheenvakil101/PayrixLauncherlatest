<Window x:Class="PayrixLauncher.CollectionSaveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save to Collection" SizeToContent="Height" Width="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        FontFamily="Segoe UI">
    <Border Background="{DynamicResource WindowBg}"
            BorderBrush="{DynamicResource CardBorder}"
            BorderThickness="1" CornerRadius="14"
            Padding="24,22">
        <StackPanel>
            <!-- Title -->
            <TextBlock Text="💾  Save to Collection" FontSize="16" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,18"/>

            <!-- Request name -->
            <TextBlock Text="Request name" FontSize="12" Foreground="{DynamicResource SubText}" Margin="0,0,0,5"/>
            <TextBox x:Name="RequestNameBox" Height="34" Padding="10,0"
                     VerticalContentAlignment="Center" FontSize="13" Margin="0,0,0,14"/>

            <!-- Collection -->
            <TextBlock Text="Save into collection" FontSize="12" Foreground="{DynamicResource SubText}" Margin="0,0,0,5"/>
            <ComboBox x:Name="CollectionCombo" Height="34" FontSize="13" Margin="0,0,0,14"
                      SelectionChanged="CollectionCombo_Changed"/>

            <!-- New collection name (only shown when "New collection…" selected) -->
            <StackPanel x:Name="NewColPanel" Visibility="Collapsed">
                <TextBlock Text="New collection name" FontSize="12" Foreground="{DynamicResource SubText}" Margin="0,0,0,5"/>
                <TextBox x:Name="NewColNameBox" Height="34" Padding="10,0"
                         VerticalContentAlignment="Center" FontSize="13" Margin="0,0,0,14"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                <Button Content="Cancel" Width="80" Height="32" Margin="0,0,8,0"
                        Click="Cancel_Click"
                        Background="{DynamicResource CardBg}"
                        Foreground="{DynamicResource SubText}"
                        BorderBrush="{DynamicResource CardBorder}" BorderThickness="1">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="Save" Width="90" Height="32"
                        IsDefault="True" Click="Save_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#6366F1" Offset="0"/>
                                        <GradientStop Color="#8B5CF6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
