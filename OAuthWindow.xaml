<Window x:Class="PayrixLauncher.OAuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Width="480" Height="620"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Border Background="#FFFFFF" CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="48" Opacity="0.24" Color="#000000" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" CornerRadius="16,16,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#007AFF" Offset="0"/>
                        <GradientStop Color="#5856D6" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <TextBlock x:Name="TitleText" Text="Sign in"
                               Foreground="White" FontWeight="SemiBold" FontSize="13"
                               FontFamily="Segoe UI Variable Text, Segoe UI, Inter"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="✕" FontSize="13" Foreground="#FFFFFF88"
                               HorizontalAlignment="Right" VerticalAlignment="Center"
                               Margin="0,0,16,0" Cursor="Hand"
                               MouseLeftButtonDown="CloseBtn_Click">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>

            <!-- WebView2 -->
            <wv2:WebView2 x:Name="WebView" Grid.Row="1"
                          NavigationStarting="WebView_NavigationStarting"/>
        </Grid>
    </Border>
</Window>
